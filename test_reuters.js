const https = require("https"); https.get("https://www.educationhom.com/websites?q=reuters.com&t=" + Date.now(), (res) => { let data = ""; res.on("data", (c) => data += c); res.on("end", () => { const imgMatch = data.match(/<img[^>]+src="([^"]+)"[^>]*alt="reuters\.com"/i); if (imgMatch) { console.log("FRONTEND: LOGO IMAGE - " + imgMatch[1]); } else if (data.includes(">r<")) { console.log("FRONTEND: FALLBACK"); } else { console.log("FRONTEND: UNKNOWN"); } }); });
