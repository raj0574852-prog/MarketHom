const https = require("https"); https.get("https://www.educationhom.com/websites?q=reuters.com&t=" + Date.now(), (res) => { let data = ""; res.on("data", (c) => data += c); res.on("end", () => { const imgMatch = data.match(/<img[^>]+src="([^"]+)"[^>]*alt="reuters\.com"/i); if (imgMatch) { console.log("FRONTEND LOGO: PASS"); console.log("SUPABASE logo_url: " + imgMatch[1]); } else { console.log("FRONTEND LOGO: FAIL"); } if (data.includes(">r<")) { console.log("FALLBACK: ACTIVE"); } else { console.log("FALLBACK: NOT ACTIVE"); } const priceMatch = data.match(/\$(\d+)/); if (priceMatch) { console.log("PRICE: " + priceMatch[1]); } }); });
